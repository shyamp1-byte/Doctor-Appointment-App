// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array

const amazoncart = []; // array is created here :: only once
amazoncart.push("phone", "charger", "pen"); //we have added 3 elements
console.log(amazoncart.length); // 3
amazoncart.push("shoe"); // we have added one more element
console.log(amazoncart.length);
console.log(amazoncart);
